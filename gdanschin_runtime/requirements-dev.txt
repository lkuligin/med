# Development-only dependencies, kept out of llm_monkeys/requirements.txt so the
# reference's runtime dependency set stays as its authors pinned it.
#
# pytest-asyncio is not optional: the suite marks 50+ coroutine tests with
# @pytest.mark.asyncio, and without the plugin pytest skips them silently
# rather than failing, so the suite would look green while testing nothing.
pytest
pytest-asyncio
