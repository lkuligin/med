# Notebook server ports. Different per machine on purpose, so a tunnel from the
# laptop to the GPU box cannot collide with a local server on the same port.
# No secrets here, so unlike remote.conf and gateway.conf this file is committed.
# JUPYTER_* environment variables override it.

JUPYTER_LOCAL_PORT=8888
JUPYTER_REMOTE_PORT=8889
